import React from 'react';
import PropTypes from 'prop-types';

Topbar.propTypes = {
    
};

function Topbar(props) {
    return (
        <div>
            This TopBar
        </div>
    );
}

export default Topbar;